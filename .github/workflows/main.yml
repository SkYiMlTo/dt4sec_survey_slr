# =============================================================================
# DEPRECATED — this workflow has been superseded by weekly_slr_scraper.yml
# =============================================================================
# Kept for reference only. The new workflow includes:
#   - Chrome/Selenium setup for Computer.org scraping
#   - API key secrets injection
#   - Artifact uploads (datasets, charts, logs)
#   - Job summaries with per-source article counts
#   - Concurrency guard to prevent overlapping runs
#
# To re-enable, change the filename back to main.yml and update the triggers.
# =============================================================================
